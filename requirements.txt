Flask==2.2.2
flask-cors==3.1.1  # If you need Cross-Origin Resource Sharing (CORS) support
gunicorn==20.1.0  # If you plan to deploy using Gunicorn as your WSGI server
requests==2.28.1  # If your Flask app makes HTTP requests (optional)
whisper==1.0.0  # If you are using Whisper for transcription
numpy==1.24.1  # If you use NumPy in conjunction with Whisper